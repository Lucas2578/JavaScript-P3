const API_URL = 'http://localhost:5678/api';
const API_ROUTES = {
    WORKS: `${API_URL}/works`,
    CATEGORIES: `${API_URL}/categories`,
    LOGIN: `${API_URL}/login`,
};

export {
    API_URL,
    API_ROUTES,
};